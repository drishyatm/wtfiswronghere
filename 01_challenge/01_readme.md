the error message pointed out the arrow and showed it as invalid syntax. 
= is assigning the value to the variable and == is verifying . Hence corrected it to 
 elif i%num1==0:
 committed the changes 